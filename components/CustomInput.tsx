import * as React from 'react';
import { TextInput } from 'react-native';

const CustomInput = () => (
  <TextInput
    style={{
      marginBottom: 10,
      padding: 5,
      borderRadius: 10,
    }}
  />
);

export default CustomInput;
